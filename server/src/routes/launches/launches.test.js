describe("Test Get /launches", () => {
  test("should  response with 200", () => {
    const response = 200;
    expect(response).toBe(200);
  });
});
describe("Test Post /Launches", () => {
  test("should response with 200", () => {});
  test("should catch missing required Porperties", () => {});
  test("should catch invalid dates", () => {});
});
